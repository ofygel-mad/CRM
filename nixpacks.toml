[variables]
PYTHONUNBUFFERED = "1"

[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "gcc", "postgresql", "libpq"]

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "pip install -r requirements/prod.txt"
]
